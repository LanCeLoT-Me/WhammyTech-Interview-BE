import { createMovie } from "./create-one";
import { getAllMovies } from "./get-all";
import { getMovie } from "./get-one";

export const movieControllers = {
  createMovie,
  getAllMovies,
  getMovie,
};
